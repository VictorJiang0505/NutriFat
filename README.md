# NutriFat

A personal nutrition tracker. Log meals, get recommendations based on your dietary data.

## Setup

1. Copy `.env.example` to `.env` and fill in your Anthropic API key
2. Run the following:

```bash
npm install
npm run dev
```

Get an API key at [console.anthropic.com](https://console.anthropic.com).
